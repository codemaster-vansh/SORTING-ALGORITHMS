#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <vector>
#include <random>

void printArr(std::vector<int> array);
std::vector<int> ArrayGen(int size, int min, int max);

#endif // UTILS_H